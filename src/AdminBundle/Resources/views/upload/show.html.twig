{% extends 'base.html.twig' %}

{% block body %}
    <h1>Upload</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ upload.id }}</td>
            </tr>
            <tr>
                <th>Ownerid</th>
                <td>{{ upload.ownerId }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ upload.type }}</td>
            </tr>
            <tr>
                <th>Ext</th>
                <td>{{ upload.ext }}</td>
            </tr>
            <tr>
                <th>Containerid</th>
                <td>{{ upload.containerId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ upload.name }}</td>
            </tr>
            <tr>
                <th>Origname</th>
                <td>{{ upload.origName }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ upload.createdAt }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('upload_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('upload_edit', { 'id': upload.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
